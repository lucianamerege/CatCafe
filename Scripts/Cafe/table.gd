extends Node
class_name Table


var position
var occupied

# constructor
func _init(pos):
	occupied = false;
	position = pos;

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
